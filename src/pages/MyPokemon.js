import React from 'react';

const MyPokemon = () => {
    return (
        <div className="w-full h-full bg-blue-300 flex justify-center items-center">
            <p>You haven't catch any</p>
        </div>
    );
};

export default MyPokemon;
